<div class="container mt-4">
  <div class="row g-0">
    <%= render 'partials/sidebar' %>
    <main class="col-md-9 ms-sm-auto col-lg-10 px-md-4">
  <div class="card shadow rounded">
    <div class="card-header bg-primary text-white d-flex justify-content-between align-items-center">
      <h5 class="mb-0">📄 Create New File</h5>
      <%= link_to "⬅️ Back", project_project_files_path(@project, path: params[:path]), class: "btn btn-light btn-sm" %>
    </div>

    <div class="card-body">
      <%= form_with url: project_project_files_path(@project), method: :post, local: true do |f| %>
        <%= hidden_field_tag "file[path]", params[:path] || "" %>

        <div class="mb-3">
          <%= label_tag :name, "📁 File Name", class: "form-label" %>
          <%= text_field_tag "file[name]", "", class: "form-control", placeholder: "e.g. main.rb", required: true %>
        </div>

        <div class="mb-3">
          <%= label_tag :content, "📝 Initial Content", class: "form-label" %>
          <%= text_area_tag "file[content]", "", rows: 12, class: "form-control", placeholder: "# Start coding here..." %>
        </div>

        <div class="d-grid gap-2">
          <%= submit_tag "🚀 Create File", class: "btn btn-success" %>
        </div>
      <% end %>
    </div>
  </div>
    </main>
  </div>
</div>
