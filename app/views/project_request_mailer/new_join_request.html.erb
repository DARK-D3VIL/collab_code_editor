<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <style>
    body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; }
    .container { max-width: 600px; margin: 0 auto; padding: 20px; }
    .header { background-color: #f8f9fa; padding: 20px; border-radius: 5px; margin-bottom: 20px; }
    .content { background-color: #ffffff; padding: 20px; border: 1px solid #dee2e6; border-radius: 5px; }
    .button { display: inline-block; padding: 10px 20px; background-color: #007bff; color: white; text-decoration: none; border-radius: 5px; margin: 10px 5px; }
    .button:hover { background-color: #0056b3; }
    .footer { margin-top: 20px; padding-top: 20px; border-top: 1px solid #dee2e6; font-size: 12px; color: #6c757d; }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <h2>New Join Request for <%= @project.name %></h2>
    </div>
    
    <div class="content">
      <p>Hello <%= @owner.username || @owner.email %>,</p>
      
      <p>You have received a new join request for your project "<strong><%= @project.name %></strong>".</p>
      
      <p><strong>Requester:</strong> <%= @requester.username || @requester.email %></p>
      <p><strong>Request Date:</strong> <%= @join_request.created_at.strftime("%B %d, %Y at %I:%M %p") %></p>
      
      <p>Please log in to your account to review and manage this request.</p>
      
      <p>
        <a href="<%= project_project_members_url(@project) %>" class="button">
          Review Request
        </a>
      </p>
    </div>
    
    <div class="footer">
      <p>This is an automated message. Please do not reply to this email.</p>
    </div>
  </div>
</body>
</html>